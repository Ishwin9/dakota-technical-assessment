[project]
name = "dakota-reports"
version = "0.1.0"
description = "Executive report generation for the energy analytics pipeline"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.0",
    "matplotlib>=3.9.0",
    "psycopg2-binary>=2.9.0",
]

[tool.uv]
package = false
